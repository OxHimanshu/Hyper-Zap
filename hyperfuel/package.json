{
  "name": "hyperfuel",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ oxhimanshu/hyperfuel",
    "create-local": "graph create --node http://localhost:8020/ oxhimanshu/hyperfuel",
    "remove-local": "graph remove --node http://localhost:8020/ oxhimanshu/hyperfuel",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 oxhimanshu/hyperfuel",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.60.0",
    "@graphprotocol/graph-ts": "0.30.0",
    "@tableland/sdk": "^4.5.3-dev.0",
    "ethers": "^5.7.2"
  },
  "devDependencies": {
    "matchstick-as": "0.5.0"
  }
}
